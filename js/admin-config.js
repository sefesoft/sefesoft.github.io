/**
 * Admin login for the manager console (client-side only).
 * Replace with a strong secret before any public deployment; determined users can
 * still read client bundles — real protection requires a server.
 */
export const ADMIN_PASSWORD = "lupulo";
